{% extends "base.html.twig" %}
{% block h1 %}Edit my password{% endblock %}

{% block body %}
	<a class="btn btn-primary m-3" href="{{ path('app_profil')}}">Back</a>

	{# pour afficher le formulaire  #}
	{{ form_start(formUser) }}
	  {{ form_row(formUser.plainPassword, {
            label: 'Password'
        }) }}

	  <input type="Submit" value='Edit' class="btn btn-primary col-12 mt-3">
	{{ form_end(formUser) }}

{% endblock %}
