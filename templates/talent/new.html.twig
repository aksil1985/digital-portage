{% extends 'base.html.twig' %}

{% block title %}New Talent{% endblock %}

{% block body %}
  <div class="m-5">
    <h1 class="btn btn-primary m-2 p-2">Create new Talent</h1>

    {{ include('talent/_form.html.twig') }}

    <a class="btn btn-secondary m-2" href="{{ path('app_talent_index') }}">Back</a>
  </div>
{% endblock %}
